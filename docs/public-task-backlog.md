# Public Task Backlog

This file is the public-safe parking place for work we want to keep in sight, but not necessarily in the `Now` section of the roadmap.

Use it for:

- tasks that are clearly valuable, but not on the current critical path
- future use cases and showcase ideas
- follow-up work that is safe to expose in a public GitHub repository

Do not use it for:

- secrets
- internal-only planning
- confidential customer notes
- environment credentials

## Content and Use Cases

- Add a detailed Azure or public cloud landing zone use case
- Add a detailed networking and security use case beyond Meraki SD-WAN
- Apply the new shared use-case template to older detail pages where it improves consistency
- Reintroduce the `Partners` and `Certifications` sections from the `baseline-original-site` one-page version into the current site information architecture

## SEO and Frontend

- Add structured data improvements across all pages
- Add stronger internal linking between service pages and detailed use cases
- Review titles and descriptions again once the next service pages exist
- Consider a visual architecture strip for the IoT use case

## Azure and Delivery

- Extend CI with accessibility, performance, or richer delivery checks once the current baseline stays stable
- Expand the public SWA Standard Bicep foundation with richer but still public-safe modules
- Decide when the public repo should promote issue seeds into live GitHub Issues

## Parking Lot

- Consider a richer architecture diagram asset if the current HTML snapshot is not expressive enough
- Evaluate when to promote backlog items into GitHub Issues
